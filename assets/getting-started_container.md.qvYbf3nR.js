import{c as t,Q as a,j as o,m as i}from"./chunks/framework.rlpw0eGz.js";const u=JSON.parse('{"title":"Bot Workspace Management","description":"","frontmatter":{},"headers":[],"relativePath":"getting-started/container.md","filePath":"getting-started/container.md","lastUpdated":1778736656000}'),r={name:"getting-started/container.md"};function n(s,e,c,d,l,h){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="bot-workspace-management" tabindex="-1">Bot Workspace Management <a class="header-anchor" href="#bot-workspace-management" aria-label="Permalink to &quot;Bot Workspace Management&quot;">​</a></h1><p>Every bot in Memoh works inside a workspace. In server deployments this is normally an isolated container, Pod, or VM-like runtime. In trusted desktop/local scenarios it may also be a local host directory. The workspace gives the bot a filesystem, command execution environment, MCP runtime, and optional graphical desktop.</p><h2 id="concept-the-bot-workspace" tabindex="-1">Concept: The Bot Workspace <a class="header-anchor" href="#concept-the-bot-workspace" aria-label="Permalink to &quot;Concept: The Bot Workspace&quot;">​</a></h2><p>The workspace acts as the bot&#39;s private computer. Within it, the bot can:</p><ul><li>store and modify files</li><li>install software through package managers when the image allows it</li><li>execute scripts and background tasks</li><li>keep state across sessions</li><li>optionally run a desktop display and headed browser</li></ul><p>The underlying runtime is selected globally with <code>[container].backend</code> in <code>config.toml</code>, with trusted local workspace support controlled separately. The official Docker Compose server deploy uses <code>containerd</code>; Docker Engine, Kubernetes, Apple, and local workspace modes are documented in <a href="/installation/workspace-backends.html">Workspace Backends</a>.</p><h2 id="workspace-tabs" tabindex="-1">Workspace tabs <a class="header-anchor" href="#workspace-tabs" aria-label="Permalink to &quot;Workspace tabs&quot;">​</a></h2><p>Bot detail pages expose workspace-related settings across several tabs:</p><table tabindex="0"><thead><tr><th>Tab</th><th>Purpose</th></tr></thead><tbody><tr><td><strong>Container</strong></td><td>Container lifecycle, snapshots, data export/import, and CDI device settings.</td></tr><tr><td><strong>Desktop</strong></td><td>Workspace display runtime, headed browser availability, live display sessions, and session cleanup.</td></tr><tr><td><strong>Network</strong></td><td>Workspace network and overlay provider status/actions.</td></tr><tr><td><strong>Tool Approval</strong></td><td>Approval settings for tools that need explicit human permission.</td></tr><tr><td><strong>Files</strong></td><td>Browse and edit the bot workspace filesystem.</td></tr><tr><td><strong>Terminal</strong></td><td>Open interactive shells inside the active workspace runtime.</td></tr></tbody></table><p>Some tabs are hidden or limited for trusted local workspaces when the feature only applies to container-backed runtimes.</p><h2 id="container-lifecycle" tabindex="-1">Container lifecycle <a class="header-anchor" href="#container-lifecycle" aria-label="Permalink to &quot;Container lifecycle&quot;">​</a></h2><p>Manage the container-backed workspace from the <strong>Container</strong> tab.</p><ul><li><strong>Create</strong>: Initialize the workspace container if it does not exist. Progress is shown through SSE during image pull and creation.</li><li><strong>Start</strong>: Launch the workspace runtime.</li><li><strong>Stop</strong>: Gracefully shut down the runtime to save resources.</li><li><strong>Delete</strong>: Remove the runtime instance.</li></ul><p>Many workspace features, such as terminal access and container display, require the runtime to be running.</p><h2 id="workspace-display" tabindex="-1">Workspace display <a class="header-anchor" href="#workspace-display" aria-label="Permalink to &quot;Workspace display&quot;">​</a></h2><p>The <strong>Desktop</strong> tab prepares and inspects the graphical workspace runtime. It checks for the desktop toolkit, Xvnc/VNC availability, browser availability, and active display sessions.</p><p>When enabled, the workspace can run a headed Chrome/Chromium browser inside the container. The Web UI display pane connects to that desktop session so you and the agent can operate the same visible browser. For the tool model, see <a href="/getting-started/browser-computer-use.html">Browser / Computer Use</a>.</p><h2 id="container-information" tabindex="-1">Container information <a class="header-anchor" href="#container-information" aria-label="Permalink to &quot;Container information&quot;">​</a></h2><p>The <strong>Container</strong> tab displays runtime data such as:</p><ul><li>container ID and status</li><li>image</li><li>host and workspace paths</li><li>active background tasks</li><li>effective CDI devices, if configured</li></ul><h2 id="advanced-provide-cdi-devices" tabindex="-1">Advanced: Provide CDI Devices <a class="header-anchor" href="#advanced-provide-cdi-devices" aria-label="Permalink to &quot;Advanced: Provide CDI Devices&quot;">​</a></h2><p>Memoh can provide host devices to a bot container through CDI (Container Device Interface). This is an advanced capability for users who want to expose host-managed devices, most commonly GPUs, to the container runtime.</p><p>In the Web UI, this capability is placed under <strong>Advanced options</strong> in the <strong>Container</strong> tab. It is optional and only needs to be configured when the bot must access CDI-backed devices from the host.</p><h3 id="configure-cdi-devices" tabindex="-1">Configure CDI Devices <a class="header-anchor" href="#configure-cdi-devices" aria-label="Permalink to &quot;Configure CDI Devices&quot;">​</a></h3><ol><li>Open the bot&#39;s <strong>Container</strong> tab.</li><li>Click <strong>Create</strong> if the container does not exist, or recreate the container if you need to change GPU settings.</li><li>Expand <strong>Advanced options</strong>.</li><li>Enable <strong>GPU</strong>.</li><li>Enter one or more CDI device names in <strong>CDI devices</strong>.</li></ol><p>You can enter CDI device names one per line or separated with commas. Common GPU-related examples:</p><ul><li><code>nvidia.com/gpu=0</code></li><li><code>nvidia.com/gpu=all</code></li><li><code>amd.com/gpu=0</code></li><li><code>amd.com/gpu=all</code></li></ul><h3 id="host-requirements" tabindex="-1">Host Requirements <a class="header-anchor" href="#host-requirements" aria-label="Permalink to &quot;Host Requirements&quot;">​</a></h3><p>Before configuring CDI devices in Memoh, the host machine must already provide working device drivers, vendor toolkit support where required, and valid CDI specs. In practice, this usually means:</p><ul><li>the host GPU works normally outside the container</li><li>CDI spec files exist under <code>/etc/cdi</code> or <code>/var/run/cdi</code></li><li>the device name you enter in Memoh matches a real CDI device on the host</li></ul><p>To discover the exact CDI device names exposed by the host, use the vendor tool on the host machine:</p><ul><li>NVIDIA: <code>nvidia-ctk cdi list</code></li><li>AMD: <code>amd-ctk cdi list</code></li></ul><p>If Memoh reports an error such as <code>unresolvable CDI devices</code>, the configured device name does not match any CDI device visible to the container runtime.</p><h3 id="important-behavior" tabindex="-1">Important Behavior <a class="header-anchor" href="#important-behavior" aria-label="Permalink to &quot;Important Behavior&quot;">​</a></h3><ul><li>CDI device settings are applied when the container is created. Updating the setting later requires recreating the container.</li><li>Stopping and starting an existing container does not change its attached CDI devices.</li><li>The container image still needs the appropriate user-space libraries and tools if you want to run CUDA or ROCm software inside the container.</li><li>After creation, the <strong>Container</strong> tab shows the effective attached CDI devices for verification.</li></ul><h2 id="snapshots" tabindex="-1">Snapshots <a class="header-anchor" href="#snapshots" aria-label="Permalink to &quot;Snapshots&quot;">​</a></h2><p>Snapshots allow you to capture the current state of the bot&#39;s container workspace and restore it later. This is useful for saving a known good configuration, versioning the runtime, or testing complex changes safely.</p><h2 id="data-export-and-import" tabindex="-1">Data Export and Import <a class="header-anchor" href="#data-export-and-import" aria-label="Permalink to &quot;Data Export and Import&quot;">​</a></h2><p>The <strong>Container</strong> tab supports exporting and importing workspace data for backup, migration, or sharing purposes.</p><ul><li><strong>Export Data</strong> packages the workspace filesystem data into a downloadable archive.</li><li><strong>Import Data</strong> extracts an uploaded archive into the workspace filesystem.</li><li><strong>Restore</strong> resets the data directory to a clean state when the filesystem has become corrupted or you want to start fresh without recreating the runtime.</li></ul><h2 id="versioning" tabindex="-1">Versioning <a class="header-anchor" href="#versioning" aria-label="Permalink to &quot;Versioning&quot;">​</a></h2><p>Memoh tracks workspace/container versions to manage the lifecycle of the bot runtime environment. Version information helps with auditing and understanding when runtime configuration changed.</p>',42)])])}const m=t(r,[["render",n]]);export{u as __pageData,m as default};
